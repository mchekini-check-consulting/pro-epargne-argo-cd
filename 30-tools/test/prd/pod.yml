apiVersion : v1
kind: Pod
metadata:
  name: nginx
  labels:
    application: nginx-test
  namespace: test-ns
spec:
  containers:
    - name: nginx-test
      image: nginx
      ports:
        - containerPort: 80
